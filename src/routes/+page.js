export const load = async ({ fetch }) => {
    // In a real app, we might fetch some initial data here if we had a session.
    // Since this app uses localStorage for user, we handle most fetching on the client.
    return {};
};
